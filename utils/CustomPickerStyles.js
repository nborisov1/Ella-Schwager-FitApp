import { StyleSheet } from 'react-native';

const sharedStyles = StyleSheet.create({
  pickerItem: {
    fontSize: 28, // Default font size for picker items
  },
  pickerItemSelected: {
    color: '#7D3C98', // Purple color for selected item
  },
});

export default sharedStyles;
